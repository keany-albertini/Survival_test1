(()=>{
  const panel=document.getElementById('inventoryPanel');if(!panel)return;
  const characterId=sessionStorage.getItem('survival_character_id')||'guest';
  const inventoryKey='survival_inventory_'+characterId,equippedKey='survival_equipped_'+characterId,quickbarKey='survival_quickbar_'+characterId;
  const defs=window.survivalItemDefs||{wood:{label:'Bois',icon:'🪵'},stone:{label:'Pierre',icon:'🪨'},berries:{label:'Baies',icon:'🫐'},water:{label:'Bocal d’eau',icon:'🫙'},sand:{label:'Sable',icon:'🏖️'},dirt:{label:'Terre',icon:'🟫'},snow:{label:'Neige',icon:'❄️'},axe:{label:'Hache',icon:'🪓'},pickaxe:{label:'Pioche',icon:'⛏️'},shovel:{label:'Pelle',icon:'🛠️'}};
  const readInv=()=>{try{return JSON.parse(localStorage.getItem(inventoryKey)||'{}')}catch{return {}}};
  const readBar=()=>{try{const q=JSON.parse(localStorage.getItem(quickbarKey)||'[]');return Array.from({length:8},(_,i)=>q[i]||'')}catch{return Array(8).fill('')}};
  const saveBar=q=>localStorage.setItem(quickbarKey,JSON.stringify(q));
  const toast=text=>{const el=document.getElementById('toast');if(!el)return;el.textContent=text;el.classList.add('show');clearTimeout(window.__barOrgToast);window.__barOrgToast=setTimeout(()=>el.classList.remove('show'),1300)};
  const style=document.createElement('style');style.textContent=`.bar-editor{margin:0 0 12px;padding:10px;border:1px solid #ffffff18;border-radius:11px;background:#09151bcc}.bar-editor-title{display:flex;justify-content:space-between;align-items:center;margin-bottom:8px;color:#f0d28e;font-size:9px;font-weight:900}.bar-editor-title small{color:#96a8ad;font-size:7px;font-weight:700}.bar-editor-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:5px}.bar-edit-slot{position:relative;min-width:0;height:50px;border:1px solid #ffffff25;border-radius:7px;background:#152027;color:#fff;padding:4px 3px 3px;text-align:center}.bar-edit-slot.selected{border-color:#f0cf75;background:#313326}.bar-edit-slot .bar-num{position:absolute;left:3px;top:2px;color:#ffffff66;font-size:7px}.bar-edit-slot .bar-icon{font-size:17px;line-height:23px;display:block}.bar-edit-slot .bar-name{display:block;font-size:6px;font-weight:900;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.bar-edit-slot .bar-qty{position:absolute;right:4px;bottom:3px;color:#f2d58e;font-size:7px}.bar-remove{position:absolute;right:2px;top:2px;width:16px;height:16px;padding:0;border:0;border-radius:50%;background:#70452fd9;color:#fff;font-size:10px;line-height:16px}`;document.head.appendChild(style);
  const bag=panel.querySelector('.bag-view');if(!bag)return;
  const editor=document.createElement('div');editor.className='bar-editor';editor.innerHTML='<div class="bar-editor-title"><span>BARRE D’ÉQUIPEMENT</span><small id="barEditHint">Touchez une case pour la déplacer</small></div><div class="bar-editor-grid" id="barEditorGrid"></div>';bag.insertBefore(editor,bag.firstChild);
  const grid=editor.querySelector('#barEditorGrid'),hint=editor.querySelector('#barEditHint');let selected=-1;
  function valid(item){const inv=readInv();return item&&defs[item]&&(inv[item]||0)>0}
  function clean(q){let c=false;q.forEach((x,i)=>{if(x&&!valid(x)){q[i]='';c=true}});if(c)saveBar(q);return q}
  function sync(){window.refreshCraftInventory?.();window.refreshGroundInventory?.();window.refreshSurvivalQuickbar?.();window.refreshInventoryManagement?.()}
  function render(){const inv=readInv(),q=clean(readBar());grid.innerHTML='';q.forEach((item,i)=>{const d=defs[item],b=document.createElement('button');b.type='button';b.className='bar-edit-slot'+(selected===i?' selected':'');b.dataset.index=i;b.innerHTML=`<span class="bar-num">${i+1}</span>${item&&d?`<span class="bar-icon">${d.icon}</span><span class="bar-name">${d.label}</span>${(inv[item]||0)>1?`<span class="bar-qty">${inv[item]}</span>`:''}<span class="bar-remove" data-remove="${i}">↩</span>`:'<span class="bar-icon">＋</span><span class="bar-name">VIDE</span>'}`;grid.appendChild(b)});hint.textContent=selected>=0?'Touchez une autre case pour déplacer':'Touchez ↩ pour déséquiper'}
  grid.addEventListener('click',e=>{const rem=e.target.closest('.bar-remove');if(rem){e.preventDefault();e.stopPropagation();const i=Number(rem.dataset.remove),q=readBar(),item=q[i];q[i]='';saveBar(q);if(item&&localStorage.getItem(equippedKey)===item)localStorage.setItem(equippedKey,'');selected=-1;sync();render();toast((defs[item]?.label||'Objet')+' déséquipé');return}const slot=e.target.closest('.bar-edit-slot');if(!slot)return;const i=Number(slot.dataset.index),q=readBar();if(selected<0){if(!q[i])return;selected=i;render();return}if(selected===i){selected=-1;render();return}const tmp=q[selected];q[selected]=q[i];q[i]=tmp;saveBar(q);selected=-1;sync();render();toast('Barre réorganisée')});
  document.getElementById('inventoryBtn')?.addEventListener('click',()=>requestAnimationFrame(render));window.refreshQuickbarOrganizer=render;render();
})();