async function enterWorld(){const id=sessionStorage.getItem('survival_character_id');const auth=await sb.auth.getUser();if(!auth.data.user||!id){location.href='characters.html';return}const result=await sb.from('characters').select('*').eq('id',id).single();if(result.error||!result.data){location.href='characters.html';return}const c=result.data;characterTitle.textContent=c.name;specialty.textContent=c.specialty||'Explorateur';life.textContent=c.life;hunger.textContent=c.hunger;thirst.textContent=c.thirst;energy.textContent=c.energy}enterWorld();