import * as THREE from 'three';
let character=null,px=0,pz=0,moveX=0,moveY=0,yaw=0,pitch=-.06,third=false,lastSave=0,lastTime=performance.now();const SIZE=900;
const scene=new THREE.Scene();scene.background=new THREE.Color(0x8fcbe1);scene.fog=new THREE.FogExp2(0xa8d4df,.0017);const camera=new THREE.PerspectiveCamera(72,innerWidth/innerHeight,.1,1800);const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;document.getElementById('game').appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xccecff,0x53623b,2.2));const sun=new THREE.DirectionalLight(0xffe0aa,2.8);sun.position.set(-180,260,100);sun.castShadow=true;scene.add(sun);
function biomeAt(x,z){if(x< -100&&z< -70)return'Montagnes enneigées';if(x>100&&z< -70)return'Désert';if(x< -90&&z>80)return'Forêt';if(x>100&&z>90)return'Marais';return'Plaines'}function heightAt(x,z){const r=Math.hypot(x,z)/(SIZE*.5),island=Math.max(0,1-r*r);let h=(Math.sin(x*.025)+Math.cos(z*.022)+Math.sin((x+z)*.014))*4*island+island*10;if(x< -100&&z< -70)h+=Math.max(0,(-x-100)*.07+(-z-70)*.05)*island;return Math.max(-2,h)}
const geo=new THREE.PlaneGeometry(SIZE,SIZE,120,120);geo.rotateX(-Math.PI/2);const pos=geo.attributes.position,colors=[],cc=new THREE.Color();for(let i=0;i<pos.count;i++){let x=pos.getX(i),z=pos.getZ(i),h=heightAt(x,z);pos.setY(i,h);const b=biomeAt(x,z);if(Math.hypot(x,z)>SIZE*.44)cc.set(0xd8bd7c);else if(b==='Montagnes enneigées')cc.set(h>20?0xe8eef0:0x74847c);else if(b==='Désert')cc.set(0xd0a45d);else if(b==='Forêt')cc.set(0x315d32);else if(b==='Marais')cc.set(0x3d5940);else cc.set(0x668c45);colors.push(cc.r,cc.g,cc.b)}geo.setAttribute('color',new THREE.Float32BufferAttribute(colors,3));geo.computeVertexNormals();const land=new THREE.Mesh(geo,new THREE.MeshStandardMaterial({vertexColors:true,roughness:1}));land.receiveShadow=true;scene.add(land);const water=new THREE.Mesh(new THREE.PlaneGeometry(1500,1500),new THREE.MeshStandardMaterial({color:0x287b9c,transparent:true,opacity:.82,roughness:.25}));water.rotation.x=-Math.PI/2;water.position.y=-1.5;scene.add(water);
function addTree(x,z,s=1){const g=new THREE.Group(),t=new THREE.Mesh(new THREE.CylinderGeometry(.7,1,7,6),new THREE.MeshStandardMaterial({color:0x59402b})),cr=new THREE.Mesh(new THREE.ConeGeometry(4.5,11,7),new THREE.MeshStandardMaterial({color:biomeAt(x,z)==='Marais'?0x294b36:0x28572e}));t.position.y=3.5;cr.position.y=10;g.add(t,cr);g.position.set(x,heightAt(x,z),z);g.scale.setScalar(s);scene.add(g)}function addRock(x,z){const m=new THREE.Mesh(new THREE.DodecahedronGeometry(2+Math.random()*3,0),new THREE.MeshStandardMaterial({color:biomeAt(x,z)==='Désert'?0x9c744c:0x66706b,roughness:1}));m.position.set(x,heightAt(x,z)+2,z);scene.add(m)}for(let i=0;i<150;i++){let x=(Math.random()-.5)*760,z=(Math.random()-.5)*760,b=biomeAt(x,z);if(Math.hypot(x,z)<390&&(b==='Forêt'||b==='Marais'||(b==='Plaines'&&Math.random()<.28)))addTree(x,z,.7+Math.random()*.65)}for(let i=0;i<90;i++){let x=(Math.random()-.5)*760,z=(Math.random()-.5)*760;if(Math.hypot(x,z)<395)addRock(x,z)}
const body=new THREE.Group(),torso=new THREE.Mesh(new THREE.CapsuleGeometry(1.8,3.4,5,8),new THREE.MeshStandardMaterial({color:0x574331})),head=new THREE.Mesh(new THREE.SphereGeometry(1.35,12,10),new THREE.MeshStandardMaterial({color:0xb9825c}));torso.position.y=4;head.position.y=7.2;body.add(torso,head);scene.add(body);function coastalSpawn(){const a=Math.random()*Math.PI*2,r=330+Math.random()*25;return{x:Math.cos(a)*r,z:Math.sin(a)*r}}
const ctx=miniMap.getContext('2d');function drawMiniMap(){const w=220,s=w/SIZE;ctx.clearRect(0,0,w,w);ctx.fillStyle='#236b82';ctx.fillRect(0,0,w,w);ctx.save();ctx.beginPath();ctx.arc(110,110,96,0,Math.PI*2);ctx.clip();ctx.fillStyle='#668c45';ctx.fillRect(14,14,192,192);ctx.fillStyle='#d0a45d';ctx.fillRect(110,14,96,96);ctx.fillStyle='#e1e7e7';ctx.fillRect(14,14,96,96);ctx.fillStyle='#315d32';ctx.fillRect(14,110,96,96);ctx.fillStyle='#3d5940';ctx.fillRect(110,110,96,96);ctx.restore();const mx=110+px*s,mz=110+pz*s;ctx.save();ctx.translate(mx,mz);ctx.rotate(-yaw);ctx.fillStyle='#ffe18c';ctx.beginPath();ctx.moveTo(0,-9);ctx.lineTo(6,7);ctx.lineTo(0,4);ctx.lineTo(-6,7);ctx.closePath();ctx.fill();ctx.strokeStyle='#111';ctx.stroke();ctx.restore()}
function updateCompass(){let deg=((yaw*180/Math.PI)%360+360)%360,dirs=['N','NE','E','SE','S','SO','O','NO'],idx=Math.round(deg/45)%8;compassStrip.innerHTML=dirs[(idx+7)%8]+' · <span class="card">'+dirs[idx]+'</span> · '+dirs[(idx+1)%8]+' &nbsp; '+Math.round(deg)+'°'}function setStat(id,fill,value){const v=Math.max(0,Math.min(100,Number(value)||0));document.getElementById(id).textContent=Math.round(v);document.getElementById(fill).style.width=v+'%'}
function updateCamera(){const ground=heightAt(px,pz),eye=ground+6.25;body.position.set(px,ground,pz);body.rotation.y=yaw;const look=new THREE.Vector3(Math.sin(yaw)*Math.cos(pitch),Math.sin(pitch),Math.cos(yaw)*Math.cos(pitch));if(third){body.visible=true;const target=new THREE.Vector3(px,eye,pz),desired=new THREE.Vector3(px-Math.sin(yaw)*10,eye+3.8,pz-Math.cos(yaw)*10);camera.position.lerp(desired,.28);camera.lookAt(target.clone().add(look.multiplyScalar(3)))}else{body.visible=false;camera.position.set(px,eye,pz);camera.lookAt(camera.position.clone().add(look))}topinfo.textContent=(character?.name||'Survivant')+' • '+biomeAt(px,pz)+' • '+(sessionStorage.getItem('survival_server_name')||'Serveur');updateCompass();drawMiniMap()}
async function savePosition(force=false){if(!character)return;const n=Date.now();if(!force&&n-lastSave<3000)return;lastSave=n;await sb.from('characters').update({position_x:px,position_y:pz,biome:biomeAt(px,pz),has_spawned:true}).eq('id',character.id)}function loop(now){const dt=Math.min(.035,(now-lastTime)/1000||.016);lastTime=now;if(Math.abs(moveX)>.02||Math.abs(moveY)>.02){const fX=Math.sin(yaw),fZ=Math.cos(yaw),rX=Math.cos(yaw),rZ=-Math.sin(yaw),speed=34;px+=(fX*moveY+rX*moveX)*speed*dt;pz+=(fZ*moveY+rZ*moveX)*speed*dt;const d=Math.hypot(px,pz);if(d>390){px*=390/d;pz*=390/d}savePosition()}updateCamera();renderer.render(scene,camera);requestAnimationFrame(loop)}
function setupControls(){let moveTouch=null,lookTouch=null,cx=0,cy=0,lx=0,ly=0;joystick.addEventListener('touchstart',e=>{const t=e.changedTouches[0],r=joystick.getBoundingClientRect();moveTouch=t.identifier;cx=r.left+r.width/2;cy=r.top+r.height/2},{passive:false});joystick.addEventListener('touchmove',e=>{for(const t of e.changedTouches)if(t.identifier===moveTouch){let dx=t.clientX-cx,dy=t.clientY-cy,d=Math.hypot(dx,dy)||1,m=Math.min(38,d),power=Math.min(1,d/34);moveX=dx/d*power;moveY=-dy/d*power;stick.style.transform=`translate(${dx/d*m}px,${dy/d*m}px)`}e.preventDefault()},{passive:false});function moveEnd(e){for(const t of e.changedTouches)if(t.identifier===moveTouch){moveTouch=null;moveX=moveY=0;stick.style.transform=''}}joystick.addEventListener('touchend',moveEnd);joystick.addEventListener('touchcancel',moveEnd);renderer.domElement.addEventListener('touchstart',e=>{for(const t of e.changedTouches){if(t.clientX>innerWidth*.38&&lookTouch===null){lookTouch=t.identifier;lx=t.clientX;ly=t.clientY;break}}},{passive:false});renderer.domElement.addEventListener('touchmove',e=>{for(const t of e.changedTouches)if(t.identifier===lookTouch){const dx=t.clientX-lx,dy=t.clientY-ly;yaw-=dx*.0042;pitch=Math.max(-1.15,Math.min(1.05,pitch-dy*.0036));lx=t.clientX;ly=t.clientY}e.preventDefault()},{passive:false});function lookEnd(e){for(const t of e.changedTouches)if(t.identifier===lookTouch)lookTouch=null}renderer.domElement.addEventListener('touchend',lookEnd);renderer.domElement.addEventListener('touchcancel',lookEnd);viewBtn.onclick=()=>{third=!third;viewBtn.textContent=third?'3e PERSONNE':'1re PERSONNE'}}
async function enterWorld(){const id=sessionStorage.getItem('survival_character_id'),auth=await sb.auth.getUser();if(!auth.data.user||!id){location.href='characters.html';return}const r=await sb.from('characters').select('*').eq('id',id).single();if(r.error||!r.data){location.href='characters.html';return}character=r.data;setStat('life','lifeFill',character.life);setStat('hunger','hungerFill',character.hunger);setStat('thirst','thirstFill',character.thirst);setStat('energy','energyFill',character.energy);if(character.has_spawned&&character.position_x!=null){px=character.position_x;pz=character.position_y;spawnText.textContent='Retour dans '+biomeAt(px,pz)+'...'}else{const p=coastalSpawn();px=p.x;pz=p.z;spawnText.textContent='Nouveau survivant — arrivée sur la côte...';await savePosition(true)}setupControls();viewBtn.textContent='1re PERSONNE';setTimeout(()=>spawn.classList.add('hide'),1000);requestAnimationFrame(loop)}addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});addEventListener('pagehide',()=>savePosition(true));enterWorld();